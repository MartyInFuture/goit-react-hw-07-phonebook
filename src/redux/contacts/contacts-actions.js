import { createAction } from '@reduxjs/toolkit';

export const add = createAction('contacts/Add');
export const remove = createAction('contacts/Remove');
export const check = createAction('contacts/check');
